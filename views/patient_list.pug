extends layout

block content
  h1= title
  
  ul
  each patient in patient_list
    li 
      a(href=patient.url) #{patient.name}  
      | (#[strong Diagnosis:] #{patient.diagnosis}) - 
      | (#[strong Lifespan:] #{patient.lifespan})

  else
    li There are no patients.
